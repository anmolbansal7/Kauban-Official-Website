import styles from "./styles.module.css";

const Cursor = () => {
  return (
    <div className={styles.cursor}>
      <p className={styles.cursorText}>Detail</p>
    </div>
  );
};

export default Cursor;
